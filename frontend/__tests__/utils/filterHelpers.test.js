import {describe,it,expect}from "vitest";import {filterBooksBySearch,filterUsersByMode}from "../../src/utils/filterHelpers";
describe("filterHelpers Utils",()=>{const books=[{title:"React Guide",author:"FB"},{title:"Vite Guide",author:"Vite"}];it("filters books by title correctly",()=>{expect(filterBooksBySearch(books,"React")).toHaveLength(1);expect(filterBooksBySearch(books,"Guide")).toHaveLength(2);});it("handles empty search term",()=>{expect(filterBooksBySearch(books,"")).toHaveLength(2);});it("handles null or undefined books",()=>{expect(filterBooksBySearch(null,"test")).toEqual([]);});});
