import React from "react";import {render}from "@testing-library/react";import {configureStore}from "@reduxjs/toolkit";import {Provider}from "react-redux";import authReducer from "../../src/store/slices/authSlice";import bookReducer from "../../src/store/slices/bookSlice";import borrowReducer from "../../src/store/slices/borrowSlice";import userReducer from "../../src/store/slices/userSlice";import popupReducer from "../../src/store/slices/popUpSlice";
export function renderWithProviders(ui,{preloadedState={},store=configureStore({reducer:{auth:authReducer,book:bookReducer,borrow:borrowReducer,user:userReducer,popup:popupReducer},preloadedState}),...renderOptions}={}){function Wrapper({children}){return <Provider store={store}>{children}</Provider>;}return{store,...render(ui,{wrapper:Wrapper,...renderOptions})};}
