import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import ForgotPassword from "../../src/pages/ForgotPassword";import {renderWithProviders}from "../utils/test-utils";import {BrowserRouter}from "react-router-dom";
describe("ForgotPassword Page",()=>{it("renders email input and submit button",()=>{renderWithProviders(<BrowserRouter><ForgotPassword/></BrowserRouter>);expect(screen.getByPlaceholderText("EMAIL ADDRESS")).toBeInTheDocument();expect(screen.getByText("SEND OTP")).toBeInTheDocument();});it("enables button when email is present",()=>{renderWithProviders(<BrowserRouter><ForgotPassword/></BrowserRouter>);const input=screen.getByPlaceholderText("EMAIL ADDRESS");const button=screen.getByText("SEND OTP");fireEvent.change(input,{target:{value:"test@example.com"}});expect(button).not.toBeDisabled();});});
