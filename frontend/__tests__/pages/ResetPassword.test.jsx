import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import ResetPassword from "../../src/pages/ResetPassword";import {renderWithProviders}from "../utils/test-utils";import {BrowserRouter}from "react-router-dom";
describe("ResetPassword Page",()=>{it("renders new password and confirm password fields",()=>{renderWithProviders(<BrowserRouter><ResetPassword/></BrowserRouter>);expect(screen.getByPlaceholderText("NEW PASSWORD")).toBeInTheDocument();expect(screen.getByPlaceholderText("CONFIRM NEW PASSWORD")).toBeInTheDocument();expect(screen.getByText("RESET")).toBeInTheDocument();});it("enables button when both fields are present",()=>{renderWithProviders(<BrowserRouter><ResetPassword/></BrowserRouter>);const pass=screen.getByPlaceholderText("NEW PASSWORD");const confirm=screen.getByPlaceholderText("CONFIRM NEW PASSWORD");const button=screen.getByText("RESET");fireEvent.change(pass,{target:{value:"newpass123"}});fireEvent.change(confirm,{target:{value:"newpass123"}});expect(button).not.toBeDisabled();});});
