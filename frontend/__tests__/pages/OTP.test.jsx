import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import OTP from "../../src/pages/OTP";import {renderWithProviders}from "../utils/test-utils";import {BrowserRouter}from "react-router-dom";
describe("OTP Page",()=>{it("renders input field and verify button",()=>{renderWithProviders(<BrowserRouter><OTP/></BrowserRouter>);expect(screen.getByPlaceholderText("ENTER YOUR OTP")).toBeInTheDocument();expect(screen.getByText("VERIFY OTP")).toBeInTheDocument();});it("enables button when otp is entered",()=>{renderWithProviders(<BrowserRouter><OTP/></BrowserRouter>);const input=screen.getByPlaceholderText("ENTER YOUR OTP");const button=screen.getByText("VERIFY OTP");fireEvent.change(input,{target:{value:"123456"}});expect(button).not.toBeDisabled();});});
