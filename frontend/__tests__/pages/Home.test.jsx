import React from "react";import {screen}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import Home from "../../src/pages/Home";import {renderWithProviders}from "../utils/test-utils";import {BrowserRouter}from "react-router-dom";
describe("Home Page",()=>{const preloadedState={auth:{user:{name:"Test User",role:"Admin"},isAuthenticated:true,loading:false},book:{books:[]},borrow:{allBorrowedBooks:[]},user:{users:[]},popup:{}};it("renders sidebar and content",()=>{renderWithProviders(<BrowserRouter><Home/></BrowserRouter>,{preloadedState});expect(screen.getByText("Dashboard")).toBeInTheDocument();expect(screen.getByText("Admin Dashboard")).toBeInTheDocument();});it("renders AdminDashboard by default for Admin",()=>{renderWithProviders(<BrowserRouter><Home/></BrowserRouter>,{preloadedState});expect(screen.getByText("ADMIN DASHBOARD")).toBeInTheDocument();});});
