import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import Home from "../../src/pages/Home";import {renderWithProviders}from "../utils/test-utils";import {BrowserRouter}from "react-router-dom";
describe("Admin Management Flow",()=>{const preloadedState={auth:{user:{name:"Admin",role:"Admin"},isAuthenticated:true,loading:false},book:{books:[]},borrow:{allBorrowedBooks:[]},user:{users:[]},popup:{addNewAdminPopup:false}};it("opens Add New Admin popup when clicked from sidebar",()=>{renderWithProviders(<BrowserRouter><Home/></BrowserRouter>,{preloadedState});fireEvent.click(screen.getByText("Add New Admin"));});});
