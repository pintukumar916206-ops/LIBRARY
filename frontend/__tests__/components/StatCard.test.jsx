import React from "react";import {render,screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import StatCard from "../../src/components/dashboard/StatCard";
describe("StatCard Component",()=>{it("renders correctly with count and label",()=>{render(<StatCard count={10}label="Total Books"/>);expect(screen.getByText("10")).toBeInTheDocument();expect(screen.getByText("Total Books")).toBeInTheDocument();});it("calls onClick when clicked",()=>{const onClick=vi.fn();render(<StatCard count={10}label="Test"onClick={onClick}/>);fireEvent.click(screen.getByText("10").closest("div"));expect(onClick).toHaveBeenCalled();});});
