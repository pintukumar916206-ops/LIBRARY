import React from "react";import {screen}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import ReturnBookPopup from "../../src/popups/ReturnBookPopup";import {renderWithProviders}from "../utils/test-utils";
describe("ReturnBookPopup Component",()=>{it("renders confirmation message",()=>{renderWithProviders(<ReturnBookPopup borrowId="1"email="test@example.com"/>);expect(screen.getByText("CONFIRM RETURN")).toBeInTheDocument();expect(screen.getByText("YES")).toBeInTheDocument();expect(screen.getByText("NO")).toBeInTheDocument();});it("renders user information in confirmation",()=>{renderWithProviders(<ReturnBookPopup borrowId="1"email="test@example.com"/>);expect(screen.getByText(/test@example\.com/)).toBeInTheDocument();});});
