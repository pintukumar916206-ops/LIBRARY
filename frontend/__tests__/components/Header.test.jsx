import React from "react";import {screen}from "@testing-library/react";import {describe,it,expect}from "vitest";import Header from "../../src/layout/Header";import {renderWithProviders}from "../utils/test-utils";
describe("Header Component",()=>{const preloadedState={auth:{user:{name:"John Doe",role:"Admin"}}};it("renders user name and role",()=>{renderWithProviders(<Header/>,{preloadedState});expect(screen.getByText("John Doe")).toBeInTheDocument();expect(screen.getByText("Admin")).toBeInTheDocument();});it("displays current time and date",()=>{renderWithProviders(<Header/>,{preloadedState});const timeRegex=/\d{1,2}:\d{2}:\d{2} (AM|PM)/;expect(screen.getByText(timeRegex)).toBeInTheDocument();});});
