import React from "react";import {render,screen}from "@testing-library/react";import {describe,it,expect}from "vitest";import Avatar from "../../src/components/Avatar";
describe("Avatar Component",()=>{const user={name:"John Doe",avatar:{url:"https://example.com/avatar.jpg"}};it("renders correctly with user avatar",()=>{render(<Avatar user={user}size="md"/>);const img=screen.getByRole("img");expect(img).toHaveAttribute("src",user.avatar.url);});it("renders correctly with initials when no avatar",()=>{render(<Avatar user={{name:"John Doe"}}size="md"/>);expect(screen.getByText("JD")).toBeInTheDocument();});it("applies correct size classes",()=>{const {container}=render(<Avatar user={user}size="lg"/>);expect(container.firstChild).toHaveClass("w-20 h-20 text-2xl");});});
