import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import UserStats from "../../src/components/UserDashboard/UserStats";import {renderWithProviders}from "../utils/test-utils";
describe("UserStats Component",()=>{const stats={borrowed:2,returned:3};const setActiveTab=vi.fn();it("renders user stats cards",()=>{renderWithProviders(<UserStats stats={stats}allBooksCount={10}setActiveTab={setActiveTab}/>);expect(screen.getByText("2")).toBeInTheDocument();expect(screen.getByText("10")).toBeInTheDocument();});it("calls setActiveTab when clicked",()=>{renderWithProviders(<UserStats stats={stats}allBooksCount={10}setActiveTab={setActiveTab}/>);fireEvent.click(screen.getByText("Borrowed"));expect(setActiveTab).toHaveBeenCalledWith("borrowed");});});
