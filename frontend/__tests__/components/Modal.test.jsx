import React from "react";import {render,screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import Modal from "../../src/components/Modal";
describe("Modal Component",()=>{it("renders with title and children",()=>{render(<Modal isOpen={true}onClose={()=>{}}title="Test Modal"><div>Test Content</div></Modal>);expect(screen.getByText("Test Modal")).toBeInTheDocument();expect(screen.getByText("Test Content")).toBeInTheDocument();});it("calls onClose when close button is clicked",()=>{const onClose=vi.fn();render(<Modal isOpen={true}onClose={onClose}title="Test"><div/></Modal>);fireEvent.click(screen.getByRole("button",{name:/×/i}));expect(onClose).toHaveBeenCalled();});it("renders custom buttons",()=>{const onClick=vi.fn();const buttons=[{label:"Submit",onClick,variant:"primary"}];render(<Modal isOpen={true}onClose={()=>{}}title="Test"buttons={buttons}><div/></Modal>);fireEvent.click(screen.getByText("Submit"));expect(onClick).toHaveBeenCalled();});});
