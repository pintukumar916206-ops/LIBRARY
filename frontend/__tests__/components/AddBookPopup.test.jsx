import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import AddBookPopup from "../../src/popups/AddBookPopup";import {renderWithProviders}from "../utils/test-utils";
describe("AddBookPopup Component",()=>{it("renders all form fields",()=>{renderWithProviders(<AddBookPopup/>);expect(screen.getByPlaceholderText("TITLE")).toBeInTheDocument();expect(screen.getByPlaceholderText("AUTHOR")).toBeInTheDocument();expect(screen.getByPlaceholderText("PRICE")).toBeInTheDocument();expect(screen.getByPlaceholderText("QTY")).toBeInTheDocument();expect(screen.getByPlaceholderText("DESCRIPTION")).toBeInTheDocument();expect(screen.getByText("ADD")).toBeInTheDocument();});it("allows updating input fields",()=>{renderWithProviders(<AddBookPopup/>);const titleInput=screen.getByPlaceholderText("TITLE");fireEvent.change(titleInput,{target:{value:"Test Book"}});expect(titleInput.value).toBe("Test Book");});});
