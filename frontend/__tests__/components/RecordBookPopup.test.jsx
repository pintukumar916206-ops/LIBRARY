import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import RecordBookPopup from "../../src/popups/RecordBookPopup";import {renderWithProviders}from "../utils/test-utils";
describe("RecordBookPopup Component",()=>{const preloadedState={popup:{recordBookPopupData:"1"}};it("renders email input for recording",()=>{renderWithProviders(<RecordBookPopup/>,{preloadedState});expect(screen.getByPlaceholderText("ENTER EMAIL")).toBeInTheDocument();expect(screen.getByText("RECORD")).toBeInTheDocument();});it("allows typing email",()=>{renderWithProviders(<RecordBookPopup/>,{preloadedState});const input=screen.getByPlaceholderText("ENTER EMAIL");fireEvent.change(input,{target:{value:"test@example.com"}});expect(input.value).toBe("test@example.com");});});
