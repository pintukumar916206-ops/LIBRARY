import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import AdminStats from "../../src/components/AdminDashboard/AdminStats";import {renderWithProviders}from "../utils/test-utils";
describe("AdminStats Component",()=>{const stats={totalBooks:10,totalUsers:5,totalAdmin:2,activeInsideDueDate:3,overdue:1};const setSelectedComponent=vi.fn();it("renders all stat cards with correct counts",()=>{renderWithProviders(<AdminStats stats={stats}setSelectedComponent={setSelectedComponent}/>);expect(screen.getByText("10")).toBeInTheDocument();expect(screen.getByText("5")).toBeInTheDocument();expect(screen.getByText("2")).toBeInTheDocument();});it("calls setSelectedComponent when clicked",()=>{renderWithProviders(<AdminStats stats={stats}setSelectedComponent={setSelectedComponent}/>);fireEvent.click(screen.getByText("Total Collection"));expect(setSelectedComponent).toHaveBeenCalledWith("Books");});});
