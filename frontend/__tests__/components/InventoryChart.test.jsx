import React from "react";import {render,screen}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import InventoryChart from "../../src/components/dashboard/InventoryChart";
vi.mock("react-chartjs-2",()=>({Doughnut:()=><div data-testid="doughnut-chart"/>}));
describe("InventoryChart Component",()=>{const data={labels:["Available","Issued"],datasets:[{data:[10,5],backgroundColor:["#000","#fff"]}]};it("renders chart and legend",()=>{render(<InventoryChart data={data}/>);expect(screen.getByTestId("doughnut-chart")).toBeInTheDocument();expect(screen.getByText("INVENTORY ANALYSIS")).toBeInTheDocument();});it("renders with no data fallback message",()=>{render(<InventoryChart data={null}/>);expect(screen.getByText("INVENTORY ANALYSIS")).toBeInTheDocument();});});
