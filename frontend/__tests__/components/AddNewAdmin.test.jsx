import React from "react";import {screen,fireEvent}from "@testing-library/react";import {describe,it,expect,vi}from "vitest";import AddNewAdmin from "../../src/popups/AddNewAdmin";import {renderWithProviders}from "../utils/test-utils";
describe("AddNewAdmin Component",()=>{it("renders admin creation fields",()=>{renderWithProviders(<AddNewAdmin/>);expect(screen.getByText("NAME")).toBeInTheDocument();expect(screen.getByText("EMAIL")).toBeInTheDocument();expect(screen.getByText("PASSWORD")).toBeInTheDocument();expect(screen.getByText("ADD")).toBeInTheDocument();});it("allows file upload simulation",()=>{renderWithProviders(<AddNewAdmin/>);const file=new File(["hello"],"hello.png",{type:"image/png"});const input=screen.getByText("PHOTO").closest("label").querySelector("input");fireEvent.change(input,{target:{files:[file]}});expect(input.files[0]).toStrictEqual(file);});});
