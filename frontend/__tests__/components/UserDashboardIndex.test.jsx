import React from "react";import {screen}from "@testing-library/react";import {describe,it,expect}from "vitest";import UserDashboard from "../../src/components/UserDashboard";import {renderWithProviders}from "../utils/test-utils";
describe("UserDashboard Component",()=>{const preloadedState={auth:{user:{name:"John Doe",email:"john@example.com",role:"USER"}},book:{books:[{_id:"1",title:"Book 1",quantity:5}]},borrow:{userBorrowedBooks:[{_id:"1",returned:false,dueDate:new Date().toISOString()}]}};it("renders user stats and profile",()=>{renderWithProviders(<UserDashboard/>,{preloadedState});expect(screen.getByText("John Doe")).toBeInTheDocument();expect(screen.getByText("USER")).toBeInTheDocument();expect(screen.getByText("Borrowed")).toBeInTheDocument();});});
