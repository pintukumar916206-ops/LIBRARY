import {describe,it,expect}from "vitest";import borrowReducer,{resetBorrowSlice,fetchMyBorrowedBooks,recordBorrowedBook}from "../../src/store/slices/borrowSlice";
describe("borrowSlice",()=>{const initialState={userBorrowedBooks:[],allBorrowedBooks:[],loading:false,error:null,message:null};it("should return initial state",()=>{expect(borrowReducer(undefined,{})).toEqual(initialState);});it("should handle fetchMyBorrowedBooks.pending",()=>{const state=borrowReducer(initialState,fetchMyBorrowedBooks.pending());expect(state.loading).toBe(true);});it("should handle recordBorrowedBook.fulfilled",()=>{const state=borrowReducer(initialState,recordBorrowedBook.fulfilled({message:"Recorded"},"",""));expect(state.message).toBe("Recorded");});it("should handle resetBorrowSlice",()=>{const state={...initialState,error:"error",message:"message"};expect(borrowReducer(state,resetBorrowSlice())).toEqual(initialState);});});
